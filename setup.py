from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A repo contain whole pipeline to train and inference canopy height from sentinel 1 , sentinel 2 and GEDI data',
    author='songphon',
    license='',
)
